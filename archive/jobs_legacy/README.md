# Legacy Jobs – Deprecated

**Status: DEPRECATED.** Old transform scripts; not used by the main pipeline.

The active entry point is **`jobs/run_pipeline.py`** (uses `jobs/transform/` and `jobs/ingest/`).

These scripts can be archived or removed after confirming no CI or external references.
