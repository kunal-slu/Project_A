import pytest


@pytest.mark.skip(reason="Integration test placeholder; requires local config and datasets.")
def test_end_to_end():
    # set cfg paths to tmp_path, run run_pipeline(), assert outputs exist
    pass
